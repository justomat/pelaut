/**
 * @module
 * Pelaut — Dev server manager with Caddy HTTPS, git worktree awareness, and env subdomains.
 *
 * This is a CLI tool, install and use via the command line:
 *
 * @example Install and run
 * ```bash
 * npx jsr add @justomat/pelaut
 * pelaut start
 * pelaut add 'npm run dev'
 * ```
 */

export { };
