/**
 * @module
 * Pelaut CLI — Dev server manager with Caddy HTTPS, git worktree awareness, and env subdomains.
 *
 * @example
 * ```bash
 * # Install globally
 * npm install -g @justomat/pelaut
 *
 * # Start daemon and add a project
 * pelaut start
 * pelaut add 'npm run dev'
 *
 * # Open dashboard
 * open https://localhost:2000
 * ```
 */

/** CLI entry point — runs the commander program. */
export { };
